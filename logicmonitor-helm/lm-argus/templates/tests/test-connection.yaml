apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lm-argus.fullname" . }}-test-connection"
  labels:
    {{- include "lm-argus.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lm-argus.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

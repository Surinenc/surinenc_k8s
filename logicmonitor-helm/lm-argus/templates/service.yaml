apiVersion: v1
kind: Service
metadata:
  name: {{ include "lm-argus.fullname" . }}
  labels:
    {{- include "lm-argus.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lm-argus.selectorLabels" . | nindent 4 }}
